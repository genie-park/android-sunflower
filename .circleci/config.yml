description: 'first try'

orbs:
  apptest:
    jobs:
      run-test:
        docker:
          - image: circleci/python:3.7.1
        steps:
          - run: 
             command: |
               echo "We will now do special things"

version: 2.1
workflows:
  main:
    jobs:
      - apptest/run-test
